# HiPerT (eRisk 2026 Task 3): As-Submitted vs. v2 Cross-Encoder

**Purpose.** Clarify which implementation details were actually in the **submitted**
system versus a **post-submission v2 redesign**. Several commonly-asked details
(CORAL, ListMLE, leave-symptom-out CV, cross-encoder hyperparameters) belong to
code that did **not** exist at submission time.

## Timeline (git evidence)

| Date | Commit | What |
|------|--------|------|
| 2026-03-08 | `5001bd7` | add task 3 colab notebook |
| 2026-03-10 | `97f19e8` | notebook last touched |
| **2026-03-12** | `b922f08`, `1067429` | **prepare runs + evaluate — this is the submission** |
| 2026-03-29 | `2e5d89a` | "create new version of HIPERT for task 3" → **v2 cross-encoder (CORAL/ListMLE, LSO-CV)** |
| 2026-03-29 | `c6fbfd5` | v2 fix: move criterion to device |

The submitted runs were generated by [`notebooks/task3_colab.ipynb`](../notebooks/task3_colab.ipynb),
which trains the **v1 bi-encoder** (`SymptomConditionedEncoder`) Stage A→B, runs
`run_ensemble_inference`, and calls `generate_run(rid, config)` for runs 1–5.
The cross-encoder (`cross_encoder.py`, `trainer_v2.py`, v2 losses in `losses.py`)
was committed **17 days after** submission and was never executed in the notebook.

> ⚠️ The current `registry.py` reflects the **v2** reorganization (Run 1 = LLM_cascade,
> Run 2 = HiPerT cross-encoder, `INSALyon_*` names). At **submission** the registry had
> Run 1 = `HiPerT_full` (bi-encoder), Run 2 = `LLM_cascade`. Run files (`run1_full.py`)
> were rewritten on 2026-03-29; read them at commit `1067429` for the submitted behavior.

---

## Q&A (submitted vs. v2)

### 1. Was CORAL the submitted loss, with ListMLE dev-time only?

**Neither was submitted.** The submitted Run 1 (`HiPerT_full`) bi-encoder used
`CompositeLoss` ([losses.py:378](../src/hipert/training/losses.py#L378)):

```
L = λ₁·L_ord + λ₂·L_rank + λ₃·L_hier,   λ = (1.0, 0.5, 0.05)
```

- **Stage A** → `OrdinalCrossEntropy` (ε = 0.2 ordinal label smoothing)
- **Stage B** → `SymmetricCrossEntropy` (α = 1.0, β = 0.5; `use_symmetric_ce=True`
  for noisy silver labels — [stage_b.py:118](../src/hipert/training/stage_b.py#L118))
- plus `MarginRankingLoss` (margin 0.5) and `HierarchyRegularization` (λ_repel 0.01)

In the **v2 cross-encoder** (post-submission): CORAL is the default head and the
tie-break winner; ListMLE is the alternative variant. `select_best_variant`
([trainer_v2.py:633](../src/hipert/training/trainer_v2.py#L633)) chooses by
CV-collapse health, then NDCG@10, defaulting to CORAL on ties. So "CORAL primary,
ListMLE secondary" is correct **for v2 only** — neither shipped in the submission.

### 2. LSO-CV partition?

Leave-symptom-out, 5 folds — **v2 only, never run for the submission**.
`SYMPTOM_CV_FOLDS` at [cross_encoder_dataset.py:23](../src/hipert/training/cross_encoder_dataset.py#L23):

| Fold | Val symptoms | Theme |
|------|-------------|-------|
| 1 | 15, 16, 17, 18 | Verbal HI |
| 2 | 5, 6, 7, 8 | Motor HI (part) |
| 3 | 1, 2, 3, 4 | Organization |
| 4 | 9, 10, 11, 12 | Sustained attention |
| 5 | 13, 14, 15, 16, 17, 18 | Internal drive + verbal |

Train = complement of val per fold.

> ⚠️ **Not a clean K-fold partition.** Fold 5's val overlaps fold 1's val (15–18),
> and several symptoms appear in val multiple times. These are hand-designed
> factor-holdout folds, not standard disjoint cross-validation — describe accordingly.

### 3. How were backbone outputs combined — uniform or weighted mean?

**Uniform 1/3 mean, unweighted.** `run_ensemble_inference`
([inference.py:276](../src/hipert/training/inference.py#L276)):
`score = sum(scores) / len(scores)` over the 3 backbones
(mental-roberta, clinical-bert, mpnet). Per-backbone score is the calibrated
expected relevance `φ = Σ r·p_cal(r | s, q)`, r ∈ {0,1,2,3}. Each backbone is
scored over a 2× wider pool (`top_n*2`) before averaging and truncating to top-1000
([inference.py:246](../src/hipert/training/inference.py#L246)). v2 keeps uniform
1/3 across backbones × 1/5 across folds.

### 4. RRF k = 60?

**Yes.** `RRF_K = 60` ([run3_ensemble.py:24](../src/hipert/runs/run3_ensemble.py#L24)):
`score(s) = 1/(60 + rank_a) + 1/(60 + rank_b)`.

### 5. RRF over top-1000 ranks?

**Effectively yes.** Each input list is already capped at top-1000 (runs 1/2/5
truncate to 1000), so RRF fuses the **union of two top-1000 lists** using ranks
1…1000, then truncates the fused output to top-1000
([run3_ensemble.py:72](../src/hipert/runs/run3_ensemble.py#L72)).

> Fusion *pair* differs by version: at submission Run 3 fused Run 1 (bi-encoder full)
> + Run 2 (LLM cascade); in v2 it fuses Run 1 (LLM) + Run 2 (cross-encoder), falling
> back to Run 5 (BiEnc) if Run 2 is missing.

### 6. Cross-encoder training hyperparameters

`TrainerV2Config` ([trainer_v2.py:42](../src/hipert/training/trainer_v2.py#L42)) —
**v2, not in the submission**:

- Backbone: mpnet default; ensemble of all 3. Input `[CLS] symptom [SEP] sentence [SEP]`, CLS-pooled.
- Head: MLP 768 → 256 (GELU), dropout 0.3 then 0.1; CORAL out = 3 thresholds / ListMLE out = 1.
- `num_unfrozen = 4` top layers; embeddings always frozen.
- Optimizer AdamW, **lr 2e-5**, **weight_decay 0.01**; CosineAnnealingLR, `T_max = max_epochs`, `eta_min = lr × 0.1`.
- **max_epochs 20**, **batch_size 64**, **max_grad_norm 1.0**, **max_length 256**, AMP mixed precision.
- Early stop on NDCG@10, **patience 3**.
- CORAL: `threshold_weights = None` (uniform), `use_confidence_weighting = True`.
- ListMLE: temperature 1.0, sublist_size 64, num_sublists_per_batch 8, steps_per_epoch 200.
- CV: 5 LSO folds → final score averaged over **3 backbones × 5 folds**
  (per [run2_llm.py:9](../src/hipert/runs/run2_llm.py#L9) docstring).

---

## Bottom line for the paper

If describing the **eRisk 2026 submission**, the ranking model was the **bi-encoder**
with the **OrdinalCE / SymmetricCE + margin + hierarchy composite loss** and **uniform
3-backbone averaging**. CORAL, ListMLE, the LSO-CV folds, and the cross-encoder
hyperparameters all describe a **post-submission v2** that was never run in the
submission notebook. **RRF k = 60** and the **top-1000 fusion** apply to both versions.
