# eRisk 2026 Task 2 — Results Analysis (INSA-Lyon)

**Task.** Contextualized early detection of depression: for each of 523 Reddit users, decide whether they are at risk of depression based on streamed discussion threads (full conversational context, not isolated posts). Decisions can be issued at any round; latency is penalized.
**Team.** INSA-Lyon. Coverage: full 500/500 user threads, lapse 6 days 01:45.
**Pipeline (shared across all 5 runs).** Three-layer feature extraction (1920-d sentence-transformer ensemble + BDI-II symptom scores + lexical / VADER / emotion / BERTopic), Wasserstein + Mahalanobis distance features, ToM features (LLM-based perception tracking), Thompson-sampled bandit selector, and a final classifier whose choice (XGBoost / NN / Ensemble) and threshold define each of the 5 runs. ~2,341-dim feature vector.

Source of all figures below: [`runs/task2_all_results.json`](../runs/task2_all_results.json), generated by [`scripts/consolidate_task2_results.py`](../scripts/consolidate_task2_results.py).

---

## 1. Official preliminary results (eRisk PDF, Tables 6 & 7)

INSA-Lyon submitted 5 runs covering all 523 subjects (per Table 5: "500 user threads" — actually 523 subjects, 91 positives / 432 controls; the "500" refers to the round count).

### 1.1 Decision-based metrics (Table 6, ordered by F1 rank in the field)

| Run | Classifier | P | R | F1 | ERDE5 | ERDE50 | latencyTP | speed | Flatency | F1 rank |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | XGBoost (baseline) | 0.76 | 0.85 | **0.80** | 0.16 | 0.07 | 13 | 0.95 | 0.76 | **5/63** (tied) |
| 1 | XGBoost (ERDE-5 oriented) | 0.69 | 0.89 | 0.78 | **0.15** | **0.05** | **9** | **0.97** | **0.75** | 8/63 |
| 2 | Neural net (recall-extreme) | 0.46 | **0.95** | 0.62 | 0.14 | 0.08 | **7.5** | **0.97** | 0.60 | 36/63 |
| 3 | Ensemble (XGB + NN + SVM) | 0.63 | 0.92 | 0.75 | 0.16 | 0.06 | 11.5 | 0.96 | 0.72 | 11/63 |
| 4 | XGBoost (baseline replicate) | 0.75 | 0.85 | **0.80** | 0.16 | 0.07 | 14 | 0.95 | 0.76 | **5/63** (tied) |

Field context for F1 (Table 6):
- **HUGETIME #0** is the field winner: F1=0.83, Flatency=0.81.
- Tied at F1=0.82: UNED-GELP #2 (Flatency=0.81), NoirByte #0 (Flatency=0.76), MindAILab #0 (Flatency=0.77).
- **Our Run 0 / Run 4 at F1=0.80 are tied for 5th in the field** (with Awakened #0 if any, etc.), beating most teams' best runs.

Field context for ERDE5 (the cost-of-late-correct-positives metric):
- The leaders are UNED-GELP #3 (0.10), UET-PsyWar #0 (0.10) — both alerting after ≤4 writings on average.
- **Run 1's ERDE5 = 0.15 is in the upper-middle.** It also has the team's best ERDE50 (0.05) and best Flatency (0.75) — so this is our best "early-conviction" run despite a slightly lower F1.

### 1.2 Ranking-based metrics (Table 7, INSA-Lyon at 500 writings)

| Run | P@10 | NDCG@10 | NDCG@100 |
|---|---:|---:|---:|
| 0 | 1.00 | 1.00 | 0.89 |
| 1 | 1.00 | 1.00 | 0.89 |
| 2 | 1.00 | 1.00 | 0.85 |
| 3 | 1.00 | 1.00 | 0.89 |
| 4 | 1.00 | 1.00 | 0.89 |

Field context: DeepCare #0 leads ranking at NDCG@100=0.91; MindAILab #0 at 0.90; NoirByte 0.89; **INSA-Lyon 0.89** — tied with NoirByte at rank 3 across 4 of our 5 runs.

**Top-of-ranking is perfect across all runs from round 100 onward** — by the time 100 threads are processed, all our runs rank the top 10 risk users correctly.

---

## 2. Reproducibility — local re-score matches the PDF

Decision-based metrics from a from-scratch re-scoring of [`runs/task2/train/decisions/`](../runs/task2/train/decisions/) against the official golden file:

| Run | F1 local | F1 PDF | Δ | ERDE5 local | ERDE5 PDF | Δ | Flatency local | Flatency PDF | Δ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 0.802 | 0.80 | +0.002 | 0.155 | 0.16 | −0.005 | 0.768 | 0.76 | +0.008 |
| 1 | 0.779 | 0.78 | −0.001 | 0.136 | 0.15 | −0.014 | 0.758 | 0.75 | +0.008 |
| 2 | 0.617 | 0.62 | −0.003 | 0.134 | 0.14 | −0.006 | 0.603 | 0.60 | +0.003 |
| 3 | 0.750 | 0.75 | 0.000 | 0.151 | 0.16 | −0.009 | 0.722 | 0.72 | +0.002 |
| 4 | 0.798 | 0.80 | −0.002 | 0.156 | 0.16 | −0.004 | 0.761 | 0.76 | +0.001 |

**All decision metrics reproduce to within ±0.01.** Two known systematic offsets:
- **`latencyTP` is exactly −1 round** across every run (12/8/6.5/10.5/13 local vs 13/9/7.5/11.5/14 PDF). This is a `k_u` definition difference: we count `k_u = first_alert_round + 1` (1-indexed writings); the organizers appear to count the new thread released at the alert round as already "seen", giving +1.
- **`NDCG@100` is ~−0.04 lower locally** at 500 writings (0.85 vs 0.89). P@10 and NDCG@10 match exactly (both 1.00). Likely a tie-breaking or IDCG-normalization convention difference (trec_eval vs custom). The fact that P@10 and NDCG@10 match perfectly while NDCG@100 diverges by a constant ~0.04 suggests the ranking *order* of the top is identical and the gap is purely in the IDCG denominator.

Unlike Task 1, the Task 2 local pipeline reproduces the official metrics — confirming that the submitted prediction streams in `runs/task2/train/decisions/` are exactly what the eRisk server scored.

---

## 3. Confusion-matrix breakdown per run

The 5 runs sit on a precision/recall continuum. Below are the local TP/FP/FN/TN counts (out of 523 subjects, 91 positives):

| Run | Classifier strategy | TP | FP | FN | TN | Alerts |
|---|---|---:|---:|---:|---:|---:|
| 0 | XGB baseline, conservative threshold | 77 | 24 | 14 | 408 | 101 |
| 1 | XGB, ERDE-5 oriented (earlier + more sensitive) | 81 | 36 | 10 | 396 | 117 |
| 2 | NN, recall-extreme | 86 | **102** | **5** | 330 | **188** |
| 3 | Ensemble | 84 | 49 | 7 | 383 | 133 |
| 4 | XGB baseline replicate (variant threshold) | 77 | 25 | 14 | 407 | 102 |

**Reading the matrix:**
- Runs 0 and 4 are near-duplicates — both XGBoost-based with conservative thresholds. They alert on ~101 users (vs 91 actual positives) and miss 14 (recall 0.85).
- Run 1 trades 12 extra false alerts for 4 extra true positives, plus alerts ~4 rounds earlier on average (latencyTP 8 vs 12-13). That earlier-alerting profile is what drops ERDE5 from 0.155 → 0.136 and ERDE50 from 0.070 → 0.052.
- Run 2 catches almost everyone (86 / 91 = 94.5% recall) but pays in precision: 102 false alerts is more than the total positives. Useful only as the recall-extreme of the portfolio.
- Run 3 (Ensemble) sits between Run 1 and Run 2: 84 TPs, 49 FPs, 7 FNs. Decent balance but no individual metric where it wins outright.

**Observation for the portfolio strategy:** Run 0/4 + Run 1 + Run 3 span the F1-vs-ERDE tradeoff well. Run 2 underperforms its design intent: even with 188 alerts it only catches 5 more positives than Run 1, so the marginal cost per extra TP is 12 FPs.

---

## 4. Training vs test — model selection paid off

Per-run **training-set evaluation** (from [`runs/task2/train/eval_results.json`](../runs/task2/train/eval_results.json), measured on eRisk-2025 Task 2 test data used as our train+validation):

| Run | Classifier | F1 train | F1 test (PDF) | Δ |
|---|---|---:|---:|---:|
| 0 | XGBoost | 0.70 | 0.80 | **+0.10** |
| 1 | XGBoost | 0.68 | 0.78 | **+0.10** |
| 2 | NN | 0.65 | 0.62 | −0.03 |
| 3 | Ensemble | 0.63 | 0.75 | **+0.12** |
| 4 | XGBoost | 0.70 | 0.80 | **+0.10** |

The XGBoost and Ensemble runs **generalized better on test than they scored on train** (+0.10 to +0.12 F1) — characteristic of early-detection systems that benefit from longer trajectories at test time (median test trajectory > median train trajectory). The neural net is the only run that did NOT lift on test (−0.03), consistent with its higher overfitting risk at our train scale.

`mean_alert_round` on train was 158 (Run 0), 114 (Run 1), 128 (Run 2), 114 (Run 3) — all much later than the test latencyTP of 8-13. So the test users either gave evidence faster than the train users, or our thresholds were tuned conservatively on train.

---

## 5. Ranking trajectory — when does the ranking stabilize?

NDCG@100 by checkpoint, all runs:

| Writings | Run 0 | Run 1 | Run 2 | Run 3 | Run 4 |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.63 | 0.63 | 0.54 | 0.62 | 0.62 |
| 100 | 0.83 | 0.83 | 0.80 | 0.83 | 0.83 |
| 250 | 0.86 | 0.85 | 0.82 | **0.87** | 0.86 |
| 500 | 0.85 | 0.85 | 0.80 | **0.86** | 0.84 |

- **By 100 writings, NDCG@100 reaches ~0.83** for all runs except Run 2 (0.80). Most of the ranking quality is established in the first 20% of the stream.
- **Run 3 (Ensemble) peaks at 250 writings (0.87)** then dips slightly at 500. This is the only run with a clear peak before the end, suggesting the ensemble is most discriminative mid-stream and gets less informative as more posts accumulate (possibly noise in late posts).
- **Run 2's ranking drops between 250 and 500** (0.82 → 0.80) — the same NN that drives the recall-extreme decision strategy also adds late-stream noise to its scores.

P@10 and NDCG@10 are 1.000 from 100 writings onward across all 5 runs — the top 10 are nailed.

---

## 6. Runtime / latency engineering — why per-round response time varied

The live server loop processes one round per server release; the system must return a decision for every subject before the next round unlocks. Two encoding bottlenecks in the original `run_pipeline()` ([src/erisk_task2/pipeline.py](../src/erisk_task2/pipeline.py)) dominated wall-clock time, and both were fixed in commit `c6f6cbc` (developed during the test window, committed 1 May).

### The two bottlenecks

1. **One encode call per user, not batched.** The original loop iterated subjects and called `encoder.encode(target_texts)` separately for each of the ~522 subjects (plus a separate call per title) — ~1,000+ tiny forward passes through the **3-model sentence-transformer ensemble** every round, with poor batch utilization.
2. **Encoding every other participant's comments each round.** For the ToM "observer view" feature, the old `process_thread` ran `encoder.encode(other_texts).mean(axis=0)` over **all** non-target comments in the thread — purely to produce one mean embedding. Per the test-set EDA, context volume is enormous (mean 48 comments/thread, max 5,093; positive subjects sit in discussions with a median ~13,700 context comments), so this encoded tens of thousands of context texts per round. The fix comment names it directly: *"only used for ToM observer view and add ~10x encoding overhead."*

### The fix (`c6f6cbc`)

- **Batch once per round** ([pipeline.py:995-1038](../src/erisk_task2/pipeline.py#L995-L1038)): collect all subjects' target texts + titles into a single list, run **one** `encoder.encode(all_texts)`, slice the result back per subject.
- **Skip `other_comments` in live mode** ([pipeline.py:222](../src/erisk_task2/pipeline.py#L222)): the observer-view feature is set to `None`; only target writings + title are encoded.
- **Skip BERTopic in live mode** ([pipeline.py:936](../src/erisk_task2/pipeline.py#L936)): it loads its own 4th sentence-transformer and OOM'd alongside the 3 encoders; its 41 features are zeroed.
- Added `Encoding N texts: Xs` / `Process threads: Xs` timing logs so per-round latency became measurable.

(The offline-eval analogue is the `precompute_embeddings` Phase-1/Phase-2 split in commits `e388cb1` + `887dbbb`.)

### How this explains the per-round profile

Measured per-round encode times from the live run log ([runs/task2/train/run.log](../runs/task2/train/run.log), 405 timed rounds):

| Round | Texts encoded | Encode time |
|---|---:|---:|
| Round 0 (first) | 19,832 | **2,416 s (~40 min)** |
| Early rounds | ~678 | ~100–120 s |
| Late rounds | 10–14 | 2.5–7 s |

Mean 358 texts/round → 53.9 s; min 2.5 s; max 2,416 s.

- **Round 0 is the spike** because the first release delivers every subject's initial backlog of writings at once (~38 target texts × 522 subjects ≈ 19,832). Even after the fix it's ~40 min; *before* the fix it would also have dragged in all context comments (hundreds of thousands of texts) — the "encoding took too long at first" symptom.
- **Later rounds get faster** because each round only adds the next thread per subject and subjects exhaust their streams — active subjects decay **522 → 12** over the run (see [§ test-set EDA](task2_test_eda.md)). Encode time tracks that decay almost linearly.
- **Before the fix, round-to-round time was erratic** because it was dominated by how many other-participant comments happened to land in that round's threads (highly variable). After the fix, per-round time depends only on the number of *target* writings, making latency smooth and letting early rounds finish in seconds — directly relevant to the ERDE5 / latency-TP results, since Run 1's median alert at ~8–9 writings lands inside the high-coverage early window.

---

## 7. Insights for the working notes

1. **Two of our five runs (R0, R4) tied for 5th in the field on F1 (0.80) and 3rd on NDCG@100 (0.89).** This is our strongest cross-task result; lead with it.
2. **Run 1 is our best "early" run** — lowest ERDE5/ERDE50, lowest latencyTP, best Flatency. Trades 0.02 F1 for materially earlier alerts. Worth showcasing as a separate operating point on the precision/recall/latency frontier.
3. **The Ensemble (Run 3) underperforms its individual components on F1** but provides the cleanest ranking trajectory (peaks at 250 writings, NDCG@100 = 0.87). Suggests ensemble combination is helping ranking calibration more than binary decision quality.
4. **Run 2 (NN) is the recall-extreme outlier.** Defensible as part of the portfolio but doesn't carry its own weight on F1 / Flatency. Consider in the working notes whether it earns its slot.
5. **Cross-run agreement: identical confusion matrices for Run 0 and Run 4** (77/24/14/408 vs 77/25/14/407 — single FP difference). Worth verifying these aren't accidentally the same XGBoost model with a 1-subject threshold tweak; if so, Run 4 should be replaced with something more differentiated in future submissions.
6. **Train → test F1 lift of +0.10 to +0.12** on XGBoost / Ensemble — strong evidence that longer trajectories help. Implies our system is *under-confident* at low writing counts and a more aggressive early threshold could improve ERDE5 without much F1 loss.

---

## 8. Open questions / follow-up experiments

- **Are Run 0 and Run 4 essentially the same classifier with a 1-subject difference?** Quick diff of the predicted alert sets across all 500 rounds would confirm. If yes, swap Run 4 next year for a different operating point (e.g. lower threshold + ToM-features-off ablation).
- **Late-stream noise in Runs 2 and 3.** NDCG@100 peaks at 250 writings then drops. Worth investigating whether re-ranking with an earlier checkpoint would help in a production setting.
- **Earliest-alert subset.** Run 1 alerts at median k_u=8 (PDF=9), 12-position improvement over Run 0. Identifying the ~30 subjects where Run 1 alerts ≥5 rounds earlier than Run 0 (and what feature signal trips it) would be a useful qualitative section.
- **Pull from Colab:** the `output/{logs, training_v2}` folders on Drive should contain per-epoch training history we don't have locally (we only kept the final `eval_results.json` + classifier pickles). Useful for a "training dynamics" subsection.
